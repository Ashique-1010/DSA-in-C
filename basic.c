/*
comment
explain
*/
// comment


#include <stdio.h>

// data types

void main()
{
    int a, b;
    printf("Enter value of a and b: ");
    scanf("%d %d",&a ,&b);
    printf("Value of a = %d b = %d\n", a, b);

    if(a > 6)
    {
        printf("a is greater than 6");
    }
    else if(a == 6)
    {
        printf("a equal to 6");
    }
    else
    {
        printf("a is less than 6");
    }

    for(intitla condition; end condition; increment)


}
